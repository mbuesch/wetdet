[package]
name = "sdlog"
version = "1.0.0"
edition = "2024"
publish = false

[dependencies]
heapless = "0.9"
rkyv = { version = "0.8", features = [ "unaligned" ] }

[features]
default = []
debug = []
